import { AppShell } from '@/components/app-shell';
import { TimeManagementTips } from '@/components/time-management-tips';

export default function TipsPage() {
  return (
    <AppShell>
      <TimeManagementTips />
    </AppShell>
  );
}
